import * as React from "react";
import Layout from "@components/dao/Layout";

const Vote: React.FC = () => {
  return <Layout>Current vote for individual proposal here..</Layout>;
};

export default Vote;
// export const getStaticPaths = paths;
// export const getStaticProps = props;
