import Layout from "@components/dao/Layout";
import * as React from "react";

const CreateDistribution: React.FC = () => {
  return <Layout>Create Distribution here...</Layout>;
};

export default CreateDistribution;
